[package]
name = "toml"
version = "0.1.0"
edition = "2024"

[dependencies]
thiserror = "2.0.18"
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
insta = { version = "1.48.0", features = ["glob","json"] }
